# Восстановление USB/SMB — 2026-08-31

## Контрольная точка до действий

- Источник: скриншот пользователя, `22:28:11`.
- Веб-интерфейс: раздел «Приложения» → «Диски и принтеры».
- USB-том `OPKG`: `EXT4`, `USB 3.0`, статус `ОТКЛЮЧЕНО`, доступна кнопка «Переподключить».
- Встроенное хранилище: `UBIFS`, статус `ПОДКЛЮЧЕНО`.
- «Сервер SMB»: статус `ОТКЛЮЧЕНО`; указано «Открыт доступ без пароля».
- «Личное облако / WebDAV»: `ОТКЛЮЧЕНО`.
- Изменения роутера из этого сеанса не выполнялись.
- Read-only SSH-проверка `root@192.168.1.1` не прошла: `Connection refused`.

## Безопасная последовательность восстановления

1. Не отключать встроенное хранилище и не форматировать диск.
2. Нажать «Переподключить» только в строке `OPKG`.
3. Дождаться статуса `ПОДКЛЮЧЕНО`.
4. Включить переключатель «Сервер SMB».
5. Проверить доступ из Finder: `Cmd+K` → `smb://192.168.1.1`.

## Если связь с чатом пропадёт

- Не нажимать повторно несколько раз и не перезагружать роутер сразу.
- Подождать 2–3 минуты: переподключение USB/OPKG может временно остановить зависимые сервисы.
- После восстановления сети вернуться к этому файлу и продолжить с последнего подтверждённого статуса.
- Если `OPKG` останется отключённым, открыть «Диагностика» → «Системный журнал» и сохранить/сфотографировать ошибки; диск не форматировать.

## Журнал действий

| Время | Действие | Результат |
|---|---|---|
| 22:28:11 | Зафиксировано исходное состояние | `OPKG` отключён, SMB отключён, встроенное хранилище подключено |

## Подтверждённые события по присланным логам и скриншотам

- `22:23:10–22:23:16`: SMB включён, `OPKG` смонтирован, XKeen запущен в режиме `Hybrid`, SMB-сервис стартовал.
- `22:23:40–22:27:34`: несколько повторных остановок/запусков: при размонтировании `OPKG` останавливались XKeen, Dropbear и SMB; затем они запускались снова.
- `22:33:01–22:33:41`: последняя подтверждённая последовательность — `OPKG` смонтирован, XKeen запущен в `Hybrid`, SMB включён и сервис стартовал.
- `22:37`: Finder открыл том `OPKG` через `192.168.1.1`; это подтверждает доступ к SMB, но не SSH.
- `22:37`: браузер получил `ERR_TIMED_OUT` для `chatgpt.com` и `youtube.com`; ICMP до `chatgpt.com` прошёл без потерь, поэтому ping не подтверждает рабочий HTTPS-путь.
- Текущая read-only SSH-проверка с Mac получила `Connection refused` на `192.168.1.1:22`.
- В self-test есть исторические успешные SSH-входы по ключу с `192.168.1.94` 25 и 29 августа; значит, SSH работал раньше, но текущая доступность не гарантирована.

## Текущий доступ с Mac после восстановления

- `TCP/22` (SSH): `Connection refused`.
- `TCP/23` (встроенный Telnet CLI): порт доступен.
- `TCP/80` и `TCP/443` (веб-интерфейс роутера): порты доступны.
- `TCP/445` (SMB): в момент проверки порт отказал; Finder-подключение на скриншоте относится к более раннему моменту и не считается текущей проверкой.
- Эффективная SSH-конфигурация Mac использует порт `22`; пароль/ключи в этот лог не записываются.
- Web CLI в `22:46` вернул `no such command: ssh`; встроенный SSH-компонент Keenetic не установлен или не предоставлен в текущем наборе компонентов.

## Следующая точка восстановления доступа

Использовать локальный Web CLI `http://192.168.1.1/a` или локальный Telnet CLI. Не включать/выключать `OPKG` повторно до проверки SSH: его размонтирование останавливает Dropbear и XKeen.

Следующий шаг: в веб-интерфейсе открыть «Системные настройки» → «Обновление и набор компонентов» → «Изменить набор компонентов» и установить компонент «SSH-сервер». После установки проверить TCP/22; перезагрузку выполнять только если её явно потребует интерфейс.
